# ── Secrets & on-disk state (NEVER commit these) ──
.env
qbo_tokens.json
*.db
*.db-journal

# ── Runtime upload dirs (live on the Render persistent disk, not in git) ──
batch_tickets/
material_photos/
knowledge/

# ── Python ──
venv/
__pycache__/
*.pyc

# ── OS / editor ──
.DS_Store
Thumbs.db
